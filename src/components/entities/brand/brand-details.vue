<template>
    <div class="row justify-content-center">
        <div class="col-8">
            <div v-if="brand">
                <h2 class="jh-entity-heading"><span>Brand</span> {{brand.id}}</h2>
                <dl class="row jh-entity-details">
                    <dt>
                        <span>Name</span>
                    </dt>
                    <dd>
                        <span>{{brand.name}}</span>
                    </dd>
                    <dt>
                        <span>Line Of Business</span>
                    </dt>
                    <dd>
                        <div v-if="brand.lineOfBusiness">
                            <router-link :to="{name: 'LineOfBusinessView', params: {lineOfBusinessId: brand.lineOfBusiness.id}}">{{brand.lineOfBusiness.id}}</router-link>
                        </div>
                    </dd>
                </dl>
                <button type="submit"
                        v-on:click.prevent="previousState()"
                        class="btn btn-info">
                    <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span> Back</span>
                </button>
                <router-link v-if="brand.id" :to="{name: 'BrandEdit', params: {brandId: brand.id}}" tag="button" class="btn btn-primary">
                    <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span> Edit</span>
                </router-link>
            </div>
        </div>
    </div>
</template>

<script lang="ts" src="./brand-details.component.ts">
</script>
